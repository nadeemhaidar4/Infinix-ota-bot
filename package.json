{
  "name": "quicksave-ultimate",
  "version": "3.1.0",
  "scripts": {
    "start": "node --max-old-space-size=450 server.js",
    "build": "npx yt-dlp --update-to stable || true"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "youtube-dl-exec": "^3.0.12"
  }
}
